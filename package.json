{
  "name": "media-control-panel",
  "version": "0.1.0",
  "private": true,
  "description": "A fast, lightweight command center for every media player in your browser.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "serve": "node serve.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.9",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
